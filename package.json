{
  "name": "www_dustintoff_com",
  "version": "1.0.0",
  "scripts": {
    "dev-server": "parcel index.html",
    "build": "parcel build index.html && npm run copy-config",
    "copy-config": "cp .s3uploadconfig.json dist",
    "deploy": "./scripts/deploy",
    "clean": "rm -rf dist/",
    "release": "npm run clean && npm run build && npm run deploy"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.3.1",
    "parcel": "^2.3.1",
    "posthtml-doctype": "^1.1.1",
    "posthtml-include": "^1.7.2",
    "s3-upload-custom-resource-lambda-layer": "^1.0.3"
  }
}
